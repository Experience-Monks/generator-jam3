{
  "name": "<%= projectName %>",
  "version": "0.0.0",
  "description": "<%= projectDescription %>",
  "author": {
    "name": "<%= projectAuthorName %>",
    "email": "<%= projectAuthorEmail %>",
    "url": "https://github.com/Jam3"
  },
  "license": "ISC",
  "repository": "<%= projectRepository %>",
  "dependencies": {
    "bigwheel": "^1.1.2",
    <% if (pixi) { %>"pixi": "^0.2.1",<% } %>
    <% if (threejs) { %>"three": "^0.66.27",<% } %>
    <% if (gsap) { %>"gsap": "^1.15.0",<% } %>
    <% if (gsap) { %>"gsap-promise": "^1.3.1",<% } %>
    <% if (isDOMBased) { %>"bw-domappend": "git://github.com/bigwheel-framework/bw-domappend",<% } %>
    <% if (isDOMBased) { %>"bw-handlebars": "git://github.com/MikkoH/bw-handlebars",<% } %>
    <% if (isDOMBased) { %>"bw-instantiate": "git://github.com/jam3/bw-instantiate",<% } %>
    <% if (isDOMBased) { %>"dom-append": "^0.1.1",<% } %>
    <% if (isDOMBased) { %>"domy-bind": "^0.1.0",<% } %>
    <% if (isDOMBased) { %>"dom-event": "0.0.1",<% } %>
    <% if (isDOMBased) { %>"domready": "^1.0.7",<% } %>
    <% if (isDOMBased) { %>"dom-select": "0.0.0",<% } %>
    <% if (isDOMBased) { %>"dom-style": "0.0.3",<% } %>
    <% if (isDOMBased) { %>"prepend-child": "^1.0.0",<% } %>
    "bw-modelstatic": "git://github.com/bigwheel-framework/bw-modelstatic.git",
    "bw-vm": "^1.2.0",
    "howler": "^1.1.25",
    "lodash": "^2.4.1",
    "promise": "^6.0.1",
    "signals": "^1.0.0",
    "watchjs": "0.0.0"
  },
  "devDependencies": {
  	"audiospritler": "^0.1.3",
    "brfs": "^1.2.0",
    "browserify": "^6.3.2",
    "browserify-shim": "^3.8.0",
    "clean-css": "^2.2.18",
    "fluent-ffmpeg": "^2.0.0-rc3",
    "load-grunt-tasks": "^1.0.0",
    "grunt": "^0.4.5",
    "grunt-browserify": "^3.2.0",
    "grunt-contrib-concat": "^0.5.0",
    "grunt-contrib-connect": "^0.9.0",
    "grunt-contrib-copy": "^0.7.0",
    "grunt-contrib-handlebars": "^0.9.1",
    "grunt-contrib-less": "^0.12.0",
    "grunt-contrib-watch": "^0.6.1",
    "grunt-newer": "^0.8.0",
    "grunt-pngmin": "^0.6.2",
    "time-grunt": "latest",
    "handlebars": "^2.0.0"
  },
  "browserify": {
    "transform": [
      "browserify-shim",
      "brfs"
    ]
  },
  "browserify-shim": {
    
  },
  "browser": {}
}
